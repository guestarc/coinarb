# Local default: leave DATABASE_URL unset to use SQLite.
COINARB_SQLITE_PATH=data/coinarb.sqlite

# Cloud runtime: set this in the hosting provider's secret/environment settings.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
